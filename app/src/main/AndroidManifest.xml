<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.garytokman.garyslistandroidapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:name=".GarysListApplication"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".auth.login.LoginActivity"
                android:label="LoginActivity">

        </activity>

        <activity
                android:name=".auth.signup.SignUpActivity"
                android:label="Signup">

        </activity>

        <activity
                android:name=".auth.AuthActivity"
                android:label="AuthActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity
                android:name=".status.JobsActivity"
                android:label="Jobs Activity"/>

        <activity
                android:name=".post.PostActivity"
                android:label="Post Activity"/>

        <activity
                android:name=".update.UpdateActivity"
                android:label="Update Job"/>
    </application>

</manifest>